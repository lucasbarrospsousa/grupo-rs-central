import test from 'node:test';import assert from 'node:assert/strict';import {permitted,manageUsers} from '../backend/user-access.mjs';
test('module permissions deny direct API writes outside the granted module',()=>{const a={owner:false,views:['stock','link'],writes:['link']};assert.equal(permitted(a,'/api/devices','POST'),false);assert.equal(permitted(a,'/api/integrations/link','POST'),true);assert.equal(permitted(a,'/api/devices','GET'),true);assert.equal(permitted(a,'/api/warehouse','GET'),false);assert.equal(permitted(a,'/api/integrations/sms','POST'),false);assert.equal(permitted(a,'/api/users','GET'),false);});
test('only owner can administer accounts, regardless of payload',async()=>{await assert.rejects(manageUsers(null,{owner:false},'POST',null,{}),e=>e.status===403);await assert.rejects(manageUsers(null,{owner:true},'POST',null,{username:'test',password:'long-test-password',active:true,views:['stock'],writes:['warehouse']}),e=>e.status===400);});

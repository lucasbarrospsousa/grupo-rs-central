import test from 'node:test';import assert from 'node:assert/strict';import {analyzeBulk,bulkText} from '../public/bulk-page.js';
test('bulk preserves series zeros, deduplicates and roundtrips organization',()=>{const a=analyzeBulk('024000001;GRS - 021;Claro\n024000001;GRS - 021;Claro\n024000002');assert.equal(a.total,3);assert.equal(a.duplicates,1);assert.equal(a.rows[0].serial,'024000001');assert.equal(a.errors.length,0);assert.deepEqual(analyzeBulk(bulkText(a.rows)).rows,a.rows);});
test('bulk blocks conflicting plate and ambiguous series without guessing',()=>{assert.equal(analyzeBulk('024000001 GRS - 021\n024000002 GRS - 021').errors.length,1);assert.equal(analyzeBulk('024000001 GRS - 021\n024000001 GRS - 022').errors.length,1);assert.equal(analyzeBulk('024000001 024000002').rows.length,0);assert.equal(analyzeBulk('24000001').errors.length,1);});

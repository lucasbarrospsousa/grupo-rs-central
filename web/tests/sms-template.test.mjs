import test from 'node:test';import assert from 'node:assert/strict';import {standardSms,prepareStandardSms} from '../backend/sms-template.mjs';
test('standard SMS uses exact desktop Hinova and Link profiles',()=>{for(const [apn,profile] of [['Hinova.br','hinova.br;hinova;hinova'],['linksolutions.br','linksolutions.br;link;link']]){const r=standardSms('024000001',apn);assert.equal(r.command,`ST300NTW;024000001;319H;0;${profile};grupors1.ddns.net;5940;grupors1.ddns.net;5941;#`);assert.ok(r.command.length<=160);}assert.throws(()=>standardSms('024000001','unknown'));assert.throws(()=>standardSms('123','hinova'));});
test('standard SMS queries APN for exact series and rejects unsupported branch or mismatch',async()=>{let calls=0;const service={equipment:async()=>{calls++;return{serial:'024000001',apn:'hinova.br'};}};assert.equal((await prepareStandardSms(service,'imperatriz','024000001')).apn,'hinova.br');await assert.rejects(prepareStandardSms(service,'imperatriz','024000002'));await assert.rejects(prepareStandardSms(service,'maraba','024000001'));assert.equal(calls,2);});

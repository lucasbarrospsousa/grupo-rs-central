import { stockRows, filterStock, visibleStatus, statuses, csvText } from './stock-model.js';

const escape = v => String(v ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const glyphs = { edit:'<path d="m15 3 6 6-12 12H3v-6ZM13 5l6 6"/>', trash:'<path d="M3 6h18M9 6V3h6v3M6 6l1 15h10l1-15M10 10v7M14 10v7"/>', calendar:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M7 2v6M17 2v6M3 11h18"/>', download:'<path d="M12 2v14m-5-5 5 5 5-5M4 20h16"/>', menu:'<path d="M3 5h18M3 12h18M3 19h18"/>', chart:'<path d="M3 21h18M5 18V9h3v9M11 18V5h3v13M17 18V2h3v16"/>', clear:'<path d="m3 15 10-12 8 7-10 12H8zM6 12l8 7M11 22h11"/>', check:'<circle cx="12" cy="12" r="9"/><path d="m7 12 3 3 7-7"/>' };

export function mountStock({ repo, branch, branchName, icon, showModal, notify, analyze, render }) {
  const svg = key => glyphs[key] ? `<svg class="icon" aria-hidden="true" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">${glyphs[key]}</svg>` : icon(key);
  const modal = document.querySelector('#modal');
  const page = document.querySelector('#page');
  const on = (id, event, fn) => document.getElementById(id)?.addEventListener(event, fn);
  const safe = async fn => { try { await fn(); } catch(e) { notify(e.message); } };
  const model = {query:'',status:'Todos',start:'',end:'',sort:'installed_at',direction:-1,page:1};
  const selected = new Set(); const perPage = 10; let current = [];
  const regional = branch !== 'imperatriz';
  const refreshRows = () => stockRows(repo.list(branch));
  const data = () => filterStock(refreshRows(), {...model, branch});
  const date = s => /^\d{4}-\d{2}-\d{2}/.test(s||'') ? `${s.slice(8,10)}/${s.slice(5,7)}/${s.slice(0,4)} ${s.slice(11,16)}` : '—';
  document.querySelector('.content>.top').innerHTML = `<div class="stock-app-heading">${svg('menu')}<div><strong>Estoque de equipamentos</strong><small>Cadastro, disponibilidade e situação dos rastreadores</small><small>Início › Equipamentos › Estoque</small></div></div><span class="stock-demo">Demonstração • sem banco conectado</span>`;
  const stockLink = document.querySelector('[data-route="stock"]');
  const group = document.createElement('div'); group.className='stock-nav-group'; group.innerHTML=`${icon('box')}<b>Equipamentos</b><span>⌃</span>`; stockLink.before(group);
  for (const id of ['stock','link','bulk']) document.querySelector(`[data-route="${id}"]`).classList.add('stock-subnav');
  page.innerHTML = `<div class="stock-heading"><div><div class="stock-eyebrow">GRUPO RS CENTRAL / ${escape(branchName.toUpperCase())}</div><h1>Estoque de equipamentos</h1><p>Consulta e ações conforme a filial selecionada.</p></div><div class="stock-main-actions"><button id="stock-report">${svg('download')}Gerar relatório</button>${regional ? '' : `<button id="stock-sms-monitor">Acompanhar SMS</button><button id="stock-new" class="primary">${icon('plus')}Novo equipamento</button><button id="stock-reconnect">${icon('refresh')}Reconectar</button>`}</div></div>
    <section class="stock-card"><div class="stock-card-heading"><h2>Equipamentos · ${escape(branchName.toUpperCase())}</h2><span class="stock-count" id="stock-total"></span><small>Comunicação: demonstração • API não conectada</small></div>
    <div class="stock-search-row"><div class="stock-search"><input id="stock-query" aria-label="Buscar equipamentos" placeholder="Buscar por placa, série, telefone, chip ou operadora · várias séries com ;">${icon('search')}</div><button id="stock-analyze" class="primary">${svg('chart')}Analisar baixa</button><button id="stock-paste">Colar séries</button><button id="stock-clear">${svg('clear')}Limpar</button></div>
    <div class="stock-status-filters" id="stock-status-filters"></div><div id="stock-batch" class="stock-batch" role="status" hidden></div>
    <div class="stock-period"><label>${svg('calendar')}<span>Período inicial</span><input type="date" id="stock-start" aria-label="Período inicial"></label><label>${svg('calendar')}<span>Período final</span><input type="date" id="stock-end" aria-label="Período final"></label><button id="stock-period-apply" class="primary">Aplicar</button><button id="stock-period-clear">Limpar período</button><small>Período do cadastro / atualização</small></div>
    <div class="stock-selection" id="stock-selection" hidden></div><div class="stock-table-wrap"><table class="stock-table"><thead><tr><th><input type="checkbox" id="stock-all" aria-label="Selecionar página"></th><th><button data-sort="serial">Série ↕</button></th><th>Identificação</th><th>Veículo</th><th>Tipo</th><th><button data-sort="carrier">Operadora ↕</button></th><th><button data-sort="status">Status ↕</button></th><th><button data-sort="connectivity">Conectividade ↕</button></th><th><button data-sort="installed_at">Instalação ↕</button></th><th>Ações</th></tr></thead><tbody id="stock-body"></tbody></table></div><div class="stock-pagination" id="stock-pagination"></div></section>`;
  function draw() {
    const result = data(); current = result.rows;
    model.page = Math.min(model.page,Math.max(1,Math.ceil(current.length/perPage)));
    const rows = current.slice((model.page-1)*perPage,model.page*perPage); const all = refreshRows();
    document.querySelector('#stock-total').textContent = `${all.length.toLocaleString('pt-BR')} equipamentos`;
    document.querySelector('#stock-status-filters').innerHTML = statuses.map(s => `<button data-status="${s}" class="${model.status === s ? 'active' : ''}" aria-pressed="${model.status === s}">${s} ${s === 'Todos' ? all.length : all.filter(r => visibleStatus(r,branch) === s).length}</button>`).join('');
    document.querySelectorAll('[data-status]').forEach(b => b.onclick=()=>{model.status=b.dataset.status;model.page=1;draw();});
    const batch=document.querySelector('#stock-batch'); batch.hidden=!result.batch;
    batch.textContent=`${result.requested} pesquisados · ${result.found} encontrados na base · ${current.length} exibidos · ${result.missing.length} não encontrados${result.missing.length ? ' — '+result.missing.join('; ') : ''}`;
    document.querySelector('#stock-body').innerHTML=rows.map(r=>`<tr><td><input type="checkbox" data-selected="${escape(r.id)}" aria-label="Selecionar ${r.serial}" ${selected.has(r.id)?'checked':''}></td><td><div class="stock-series"><button class="stock-pin ${r.communication==='Atualizado'?'green':r.communication==='Desligado'?'red':r.communication==='Possível GPS'?'amber':'neutral'}" data-location="${r.id}" aria-label="Localização de ${r.serial}">${icon('map')}</button><div><button class="stock-series-link" data-detail="${r.id}">${r.serial}</button><small class="${r.communication==='Atualizado'?'green':r.communication==='Desligado'?'red':r.communication==='Possível GPS'?'amber':'neutral'}">${escape(r.communication)}</small></div></div></td><td>${escape(r.identification||'—')}</td><td>${escape(r.status==='Instalado'?r.plate:'—')}</td><td>${escape(r.model)}</td><td>${escape(r.carrier)}</td><td><span class="stock-badge">${svg('check')}${escape(visibleStatus(r,branch))}</span></td><td><span class="stock-connect ${r.connectivity==='Online'?'green':r.connectivity==='Off'?'red':'neutral'}">${escape(r.connectivity)}</span></td><td><time>${date(r.installed_at)}</time></td><td><div class="stock-row-actions"><button data-edit="${r.id}" aria-label="Editar ${r.serial}">${svg('edit')}</button><button data-sms="${r.id}" aria-label="SMS para ${r.serial}">${icon('mail')}</button><button data-delete="${r.id}" aria-label="Excluir ${r.serial}">${svg('trash')}</button></div></td></tr>`).join('')||'<tr><td colspan="10" class="empty">Nenhum equipamento encontrado para estes filtros.</td></tr>';
    const totalPages=Math.max(1,Math.ceil(current.length/perPage));
    const numbers=[...new Set([1,model.page-1,model.page,model.page+1,totalPages])].filter(n=>n>0&&n<=totalPages).sort((a,b)=>a-b);
    document.querySelector('#stock-pagination').innerHTML=`<span>Mostrando ${current.length?(model.page-1)*perPage+1:0}-${Math.min(model.page*perPage,current.length)} de ${current.length} encontrados</span><button data-page="${model.page-1}" ${model.page===1?'disabled':''} aria-label="Página anterior">‹</button>${numbers.map((n,i)=>`${i&&n>numbers[i-1]+1?'<span>…</span>':''}<button data-page="${n}" class="${n===model.page?'active':''}" ${n===model.page?'aria-current="page"':''}>${n}</button>`).join('')}<button data-page="${model.page+1}" ${model.page===totalPages?'disabled':''}>Próximo</button>`;
    document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{model.page=Number(b.dataset.page);draw();document.querySelector('.stock-table-wrap').scrollTop=0;});
    document.querySelectorAll('[data-selected]').forEach(c=>c.onchange=()=>{c.checked?selected.add(c.dataset.selected):selected.delete(c.dataset.selected);syncSelection(rows);});
    document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>equipmentForm(b.dataset.edit));
    document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>remove(b.dataset.delete));
    document.querySelectorAll('[data-sms]').forEach(b=>b.onclick=()=>smsDialog(b.dataset.sms));
    document.querySelectorAll('[data-location]').forEach(b=>b.onclick=()=>details(b.dataset.location,true));
    document.querySelectorAll('[data-detail]').forEach(b=>b.onclick=()=>details(b.dataset.detail,false));
    document.querySelector('#stock-all').onchange=e=>{rows.forEach(r=>e.target.checked?selected.add(r.id):selected.delete(r.id));draw();};
    syncSelection(rows);
  }
  function syncSelection(rows) {
    const all=document.querySelector('#stock-all'); all.checked=!!rows.length&&rows.every(r=>selected.has(r.id)); all.indeterminate=rows.some(r=>selected.has(r.id))&&!all.checked;all.disabled=!rows.length;
    const bar=document.querySelector('#stock-selection');bar.hidden=!selected.size;bar.innerHTML=`<strong>${selected.size} selecionados nesta filial</strong><button id="stock-unselect">Limpar seleção</button>`;on('stock-unselect','click',()=>{selected.clear();draw();});
  }
  function pending(title,detail) {showModal(title,`<div class="gate"><strong>Integração real pendente</strong><p>${escape(detail)}</p><p>Nenhuma consulta ou envio foi realizado.</p></div>`,'compact');}
  function details(id,location) {
    const r=refreshRows().find(r=>r.id===id);showModal(location?'Localização do equipamento':'Detalhes do equipamento',`<div class="detail-list">${[['Série',r.serial],['Identificação',r.identification],['Veículo',r.plate],['Status',visibleStatus(r,branch)],['Operadora',r.carrier],['Tipo',r.model]].map(([k,v])=>`<div>${k}<b>${escape(v)}</b></div>`).join('')}</div>${location?'<div class="gate" style="margin-top:18px">Posição e mapa aguardam a API da filial. As cores desta lista são demonstrativas.</div>':''}`,'medium');
  }
  function equipmentForm(id) {
    const r=id?refreshRows().find(r=>r.id===id):{serial:'',identification:'',plate:'',model:'',carrier:'',status:'Estoque'};
    showModal(id?'Editar equipamento':'Novo equipamento',`<p class="muted">Alteração somente no cenário demonstrativo desta filial.</p><form id="stock-equipment-form"><div class="form-grid">${[['serial','Série',r.serial],['identification','Identificação',r.identification],['plate','Veículo / placa',r.plate],['model','Tipo / versão',r.model],['carrier','Operadora',r.carrier]].map(([key,label,value])=>`<label class="field">${label}<input name="${key}" value="${escape(value)}" maxlength="80" ${key==='serial'?`required pattern="[0-9]{9}" inputmode="numeric" ${id?'readonly':''}`:''}></label>`).join('')}<label class="field">Status<select name="status">${statuses.slice(1).map(s=>`<option ${s===r.status?'selected':''}>${s}</option>`).join('')}</select></label></div><div class="form-actions"><button class="primary" type="submit">Salvar demonstração</button></div></form>`,'medium');
    if(repo.real){modal.querySelector('.muted').textContent='Alteração na cópia SQL de homologação desta filial.';modal.querySelector('[type=submit]').textContent='Salvar no SQL';}
    on('stock-equipment-form','submit',e=>{e.preventDefault();safe(async()=>{const values=Object.fromEntries(new FormData(e.target));if(!/^\d{9}$/.test(values.serial))throw Error('Informe a série com 9 dígitos.');if(!id&&repo.devices.some(d=>d.branch===branch&&d.serial===values.serial))throw Error('Série já cadastrada nesta filial.');
      if(repo.real){const submit=e.target.querySelector('[type=submit]');submit.disabled=true;try{await repo.saveDevice(branch,values,id?repo.devices.find(d=>d.id===id&&d.branch===branch):null);modal.close();draw();notify('Cadastro salvo no SQL de homologação.');}finally{submit.disabled=false;}return;}
      if(id){const item=repo.devices.find(d=>d.id===id&&d.branch===branch);if(!item)throw Error('Cadastro não encontrado.');Object.assign(item,values,{version:item.version+1,updated_at:new Date().toISOString()});}
      else repo.devices.push({...values,id:crypto.randomUUID(),branch,client:'',version:1,communication:'Não consultado',connectivity:'Não consultado',installed_at:'',updated_at:new Date().toISOString()});
      repo.record(id?'Edição demonstrativa':'Cadastro demonstrativo',values.serial,branchName);modal.close();draw();notify('Cadastro atualizado apenas na demonstração.');});});
  }
  function remove(id) {if(repo.real){const current=repo.devices.find(d=>d.id===id&&d.branch===branch);showModal('Excluir equipamento da homologação?', '<p>A remoção ficará registrada no SQL de testes e poderá ser restaurada. O sistema desktop não será alterado.</p><div class="form-actions"><button id="sql-cancel">Cancelar</button><button id="sql-delete" class="danger">Confirmar exclusão</button></div>','compact');on('sql-cancel','click',()=>modal.close());on('sql-delete','click',()=>safe(async()=>{const btn=document.querySelector('#sql-delete');btn.disabled=true;try{await repo.deleteDevice(branch,current);selected.delete(id);modal.close();draw();notify('Remoção registrada no SQL de homologação.');}finally{btn.disabled=false;}}));return;}const r=refreshRows().find(r=>r.id===id);showModal('Excluir equipamento?',`<p>Remover a série <strong>${r.serial}</strong> da demonstração de ${escape(branchName)}?</p><p class="muted">Não exclui equipamentos da plataforma ou do banco real.</p><div class="form-actions"><button id="stock-cancel-delete">Cancelar</button><button id="stock-confirm-delete" class="danger">Excluir da demonstração</button></div>`,'compact');on('stock-cancel-delete','click',()=>modal.close());on('stock-confirm-delete','click',()=>{repo.devices=repo.devices.filter(d=>!(d.id===id&&d.branch===branch));repo.record('Exclusão demonstrativa',r.serial,branchName);selected.delete(id);modal.close();draw();});}
  function smsDialog(id) {const r=refreshRows().find(r=>r.id===id);showModal('Enviar comando por SMS',`<div class="selected-info"><strong>APARELHO · ${escape(branchName.toUpperCase())}</strong><p>${r.serial}</p></div><label class="field">Telefone do chip<input placeholder="DDD + telefone" inputmode="tel"></label><label class="field">Mensagem / comando<textarea rows="4" maxlength="160" placeholder="Digite a mensagem..."></textarea></label><div class="gate">Envio bloqueado até conectar e validar o gateway. Nenhum SMS será enviado nesta prévia.</div><div class="form-actions"><button disabled>Enviar mensagem</button></div>`,'medium');}
  function report() {
    const rows=current.map(r=>({...r,status:visibleStatus(r,branch)}));
    showModal('Relatório de estoque',`<p>${escape(branchName)} • ${rows.length} equipamentos no recorte • DADOS DEMONSTRATIVOS</p><div class="stock-report-table table-wrap"><table><thead><tr><th>Série</th><th>Identificação</th><th>Veículo</th><th>Operadora</th><th>Status</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.serial}</td><td>${escape(r.identification)}</td><td>${escape(r.plate)}</td><td>${escape(r.carrier)}</td><td>${escape(r.status)}</td></tr>`).join('')}</tbody></table></div><div class="form-actions"><button id="stock-report-csv">Baixar CSV</button><button id="stock-report-print" class="primary">Imprimir / salvar PDF</button></div>`);
    on('stock-report-csv','click',()=>{const url=URL.createObjectURL(new Blob([csvText(rows)],{type:'text/csv;charset=utf-8'}));const a=document.createElement('a');a.href=url;a.download=`estoque-demonstrativo-${branch}.csv`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});
    on('stock-report-print','click',()=>{document.body.classList.add('stock-print');window.print();document.body.classList.remove('stock-print');});
  }
  on('stock-query','input',e=>{model.query=e.target.value;model.page=1;draw();});
  on('stock-query','paste',e=>{const text=e.clipboardData.getData('text');if(/[\r\n]/.test(text)){e.preventDefault();e.target.value=text.replace(/[\r\n]+/g,';');model.query=e.target.value;model.page=1;draw();}});
  on('stock-clear','click',()=>{document.querySelector('#stock-query').value='';model.query='';model.page=1;draw();});
  on('stock-paste','click',()=>{showModal('Colar séries',`<p>Cole uma série por linha ou separe por ponto e vírgula. A busca será por série exata.</p><textarea id="stock-paste-text" rows="7" aria-label="Lista de séries"></textarea><div class="form-actions"><button id="stock-paste-apply" class="primary">Buscar séries</button></div>`,'compact');on('stock-paste-apply','click',()=>{model.query=document.querySelector('#stock-paste-text').value.replace(/[\r\n]+/g,';')+';';model.page=1;document.querySelector('#stock-query').value=model.query;modal.close();draw();});});
  on('stock-period-apply','click',()=>safe(()=>{const start=document.querySelector('#stock-start').value,end=document.querySelector('#stock-end').value;if(start&&end&&start>end)throw Error('A data final não pode ser anterior à data inicial.');Object.assign(model,{start,end,page:1});draw();}));
  on('stock-period-clear','click',()=>{model.start=model.end='';model.page=1;document.querySelector('#stock-start').value=document.querySelector('#stock-end').value='';draw();});
  document.querySelectorAll('[data-sort]').forEach(b=>b.onclick=()=>{model.direction=model.sort===b.dataset.sort?-model.direction:1;model.sort=b.dataset.sort;model.page=1;draw();});
  on('stock-analyze','click',analyze);on('stock-new','click',()=>equipmentForm());on('stock-report','click',report);
  on('stock-reconnect','click',()=>pending('Reconectar APIs','A reconexão das APIs será habilitada após autorização e configuração das integrações.'));
  on('stock-sms-monitor','click',()=>pending('Acompanhar SMS','A fila e as confirmações de entrega dependem do gateway conectado.'));
  draw();
}

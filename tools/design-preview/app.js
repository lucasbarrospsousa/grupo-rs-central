const $=s=>document.querySelector(s);
const paths={box:'M3 7l9-5 9 5v10l-9 5-9-5V7zm0 0l9 5 9-5M12 12v10M7 4l10 6',chip:'M7 7h10v10H7zM9 1v4m6-4v4M9 19v4m6-4v4M1 9h4m-4 6h4M19 9h4m-4 6h4',tool:'M14 6a5 5 0 00-6 6L2 18l4 4 6-6a5 5 0 006-6l-4 4-4-4 4-4z',branch:'M4 21V3h12v18M1 21h22M8 7h4M8 11h4M8 15h4M16 11h5v10',check:'M4 12l5 5L20 5',send:'M22 2L9 15m13-13l-7 20-6-7-7-6 20-7z'};
const icon=n=>`<span class="icon" aria-hidden="true"><svg viewBox="0 0 24 24"><path d="${paths[n]||paths.box}"/></svg></span>`;
const branches=['Imperatriz','Araguaína','Açailândia','Marabá'];
const statuses=['Estoque','Instalado','Estoque','Reserva','Manutenção','Estoque','Instalado','Estoque'];
const rows=Array.from({length:32},(_,i)=>({serial:'DEMO-'+String(i+1).padStart(4,'0'),plate:'EXEMPLO '+String(i+1).padStart(2,'0'),model:['RS300','ST310','RS300','RS300'][i%4],operator:['Vivo','Claro','Tim','Multioperadora'][i%4],branch:branches[Math.floor(i/8)],status:statuses[i%8]}));
let page='inicio',branch='Imperatriz',toastTimer;
const selected=()=>rows.filter(r=>r.branch===branch);
const badge=s=>`<span class="tag ${s==='Estoque'?'green':s==='Manutenção'?'orange':s==='Reserva'?'gray':''}">${s}</span>`;
function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>$('#toast').classList.remove('show'),3500)}
function modal(html){$('#dialog-body').innerHTML=html;$('#dialog').showModal()}
function head(title,sub,action=''){return `<div class="heading"><div><div class="eyebrow">GRUPO RS CENTRAL / ${branch.toUpperCase()}</div><h1>${title}</h1><p class="sub">${sub}</p></div><div class="actions">${action}</div></div>`}
function metrics(){const data=selected();return `<section class="metrics" aria-label="Resumo demonstrativo">${[['blue','box','Equipamentos na base',data.length,'Cadastros de exemplo'],['orange','chip','Disponíveis em estoque',data.filter(r=>r.status==='Estoque').length,'Prontos para a próxima operação'],['','tool','Em manutenção',1,'Acompanhamento técnico'],['navy','branch','Instalados',2,'Vinculados nesta demonstração']].map(([c,i,t,v,s])=>`<article class="metric ${c}"><div class="top">${t}${icon(i)}</div><strong>${v}</strong><small>${s}</small></article>`).join('')}</section>`}
function table(data,compact=false){return `<div class="table-wrap"><table><thead><tr><th>EQUIPAMENTO</th><th>IDENTIFICAÇÃO</th>${compact?'':'<th>OPERADORA</th>'}<th>STATUS</th><th>AÇÕES</th></tr></thead><tbody>${data.length?data.map(r=>`<tr><td><b>${r.serial}</b><small>${r.model} · ${r.branch}</small></td><td>${r.plate}<small>Registro fictício</small></td>${compact?'':`<td>${r.operator}</td>`}<td>${badge(r.status)}</td><td><button data-detail="${r.serial}" aria-label="Ver ${r.serial}">Detalhes ↗</button></td></tr>`).join(''):`<tr><td colspan="5">Nenhum exemplo corresponde aos filtros. Tente limpar a busca.</td></tr>`}</tbody></table></div>`}
function home(){return head('Visão geral da operação','Estoque, equipamentos e rotina da filial em um só lugar.','<button class="primary" data-go="estoque">Abrir estoque ↗</button>')+metrics()+`<div class="grid"><section class="panel"><div class="panel-head"><div><h2>Distribuição dos equipamentos</h2><p class="sub">Situação dos registros de demonstração</p></div><span class="tag">${branch}</span></div>${[['Estoque',4],['Instalado',2],['Reserva',1],['Manutenção',1]].map(([s,n])=>`<div class="bar-row"><span>${s}</span><div class="track"><div class="bar" style="width:${n/8*100}%;${s==='Manutenção'?'background:#ff922c':''}"></div></div><b>${n}</b></div>`).join('')}</section><section class="panel"><div class="panel-head"><div><h2>Perfil da base</h2><p class="sub">Organização para decidir com clareza</p></div></div><div class="donut-wrap"><div class="donut" role="img" aria-label="4 em estoque, 2 instalados, 2 em outros estados"><div>8<small>equipamentos</small></div></div><div class="legend"><p>Em estoque <b>4</b></p><p>Instalados <b>2</b></p><p>Outros estados <b>2</b></p></div></div></section></div><div class="grid"><section class="panel"><div class="panel-head"><h2>Equipamentos da filial</h2><button data-go="estoque">Ver todos →</button></div>${table(selected().slice(0,4),true)}</section><section class="panel"><div class="panel-head"><h2>Atenção da central</h2><span class="tag orange">Demonstração</span></div><div class="callout warm">1 equipamento em manutenção.<br>Consulte o status antes de movimentar.</div><div class="callout">4 equipamentos disponíveis no estoque desta filial.</div><button class="shortcut" data-go="cadastro"><span>Cadastro em massa<small>Organize vários aparelhos em uma etapa</small></span>→</button><button class="shortcut" data-go="sms"><span>Painel SMS<small>Comandos e mensagens em um ambiente claro</small></span>→</button></section></div>`}
function stock(){return head('Estoque de equipamentos','Mesma organização. Mais espaço para o que você precisa encontrar.','<button data-action="report">Exportar relatório</button><button class="primary" data-action="new">+ Novo equipamento</button>')+metrics()+`<section class="panel"><div class="panel-head"><div><h2>Equipamentos · ${branch}</h2><p class="sub">Busque por identificação, aparelho ou operadora</p></div><span class="tag">Dados fictícios</span></div><div class="filters"><input id="search" aria-label="Pesquisar equipamento" placeholder="Pesquisar série, identificação ou operadora…"><select id="status" aria-label="Filtrar status"><option value="">Todos os status</option>${['Estoque','Instalado','Reserva','Manutenção'].map(x=>`<option>${x}</option>`).join('')}</select><button id="clear">Limpar filtros</button></div><div id="results">${table(selected())}</div><div class="pager"><span id="count">8 registros de demonstração</span><span>Página 1 de 1</span></div></section>`}
function bulk(){return head('Cadastro em massa','O mesmo fluxo de cadastro, com revisão mais clara antes de confirmar.')+`<div class="grid"><section class="panel"><h2>Adicionar equipamentos</h2><p class="sub">Cole uma identificação por linha. Use somente exemplos nesta prévia.</p><form id="bulk-form"><div class="fields"><label>Modelo<select><option>RS300</option><option>ST310</option></select></label><label>Destino<select><option>Estoque · ${branch}</option></select></label></div><label>Lista de equipamentos<textarea id="bulk" placeholder="DEMO-0101&#10;DEMO-0102&#10;DEMO-0103"></textarea></label><div class="actions" style="margin-top:22px"><button class="primary" type="submit">Revisar lista →</button><button type="reset">Limpar</button></div></form></section><section class="panel"><h2>Uma revisão antes de salvar</h2><p class="sub">Etapas do fluxo atual, com hierarquia visual.</p><div class="setting"><div><h3>01 · Preparar</h3><p>Selecione o modelo e informe a lista.</p></div>${icon('box')}</div><div class="setting"><div><h3>02 · Conferir</h3><p>Visualize a quantidade e os duplicados.</p></div>${icon('check')}</div><div class="setting"><div><h3>03 · Confirmar</h3><p>Nesta prévia, nada é salvo no banco.</p></div>${icon('branch')}</div></section></div>`}
function sms(){return head('Painel SMS','Um espaço mais organizado para preparar mensagens e comandos.')+`<div class="grid"><section class="panel"><h2>Preparar mensagem</h2><form id="sms-form"><div class="fields"><label>Equipamento<select id="sms-device">${selected().map(r=>`<option>${r.serial}</option>`).join('')}</select></label><label>Tipo de mensagem<select id="sms-type"><option>Mensagem livre</option><option>Consulta de exemplo</option></select></label></div><label>Mensagem<textarea id="message" required maxlength="160" placeholder="Digite uma mensagem demonstrativa…"></textarea></label><div class="pager"><span>Somente simulação visual</span><span id="chars">0 / 160</span></div><div class="actions" style="margin-top:24px"><button class="primary" type="submit">Revisar envio →</button></div></form></section><section class="panel"><div class="panel-head"><h2>Prévia da mensagem</h2>${icon('send')}</div><span class="tag">SEM CONEXÃO</span><p class="sub" style="margin:18px 0">Confira como a mensagem será apresentada.</p><div class="preview-message" id="message-preview">Sua mensagem aparecerá aqui.</div><div class="callout warm" style="margin-top:22px">Nenhum SMS será enviado. A disponibilidade real continua dependente da filial e das permissões atuais.</div></section></div>`}
function config(){return head('Configurações','Preferências mais legíveis, sem alterar os recursos do sistema.')+`<div class="grid"><section class="panel"><h2>Conexões e ambiente</h2><div class="setting"><div><h3>Base selecionada</h3><p>${branch} · seleção visual no menu lateral</p></div><span class="tag">Demonstração</span></div><div class="setting"><div><h3>Integrações</h3><p>Credenciais e serviços reais não são carregados.</p></div><span class="tag gray">Desconectado</span></div><div class="setting"><div><h3>Dados locais</h3><p>Esta prévia não acessa nem modifica o estoque.</p></div><span class="tag green">Isolado</span></div><button style="margin-top:24px" data-action="config">Visualizar configuração</button></section><section class="panel"><h2>Experimentar a apresentação</h2><div class="setting"><div><h3>Reduzir animações</h3><p>Preferência apenas desta prévia, não adicionada ao sistema.</p></div><input type="checkbox" id="reduce" aria-label="Reduzir animações" ${document.body.classList.contains('reduce')?'checked':''}></div><div class="callout" style="margin-top:24px">Cards com relevo discreto, ícones vetoriais, fontes nítidas e movimento curto. As preferências de acessibilidade do computador também são respeitadas.</div></section></div>`}
function render(){page=location.hash.slice(1)||'inicio';if(!['inicio','estoque','cadastro','sms','config'].includes(page))page='inicio';$('#main').innerHTML=`<div class="page">${({inicio:home,estoque:stock,cadastro:bulk,sms,config}[page])()}</div>`;document.querySelectorAll('[data-page]').forEach(a=>{a.classList.toggle('active',a.dataset.page===page);if(a.dataset.page===page)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});if(page==='estoque'){const filter=()=>{const q=$('#search').value.toLowerCase();const s=$('#status').value;const result=selected().filter(r=>(!s||r.status===s)&&Object.values(r).join(' ').toLowerCase().includes(q));$('#results').innerHTML=table(result);$('#count').textContent=`${result.length} registros de demonstração`};$('#search').oninput=filter;$('#status').onchange=filter;$('#clear').onclick=()=>{$('#search').value='';$('#status').value='';filter()}}if(page==='cadastro')$('#bulk-form').onsubmit=e=>{e.preventDefault();const values=$('#bulk').value.trim().split(/\n/).map(s=>s.trim()).filter(Boolean);if(!values.length){toast('Informe ao menos uma identificação de exemplo.');return}modal(`<h2>Revisar cadastro demonstrativo</h2><p>${new Set(values).size} identificações únicas · ${values.length-new Set(values).size} duplicadas.</p><div class="callout">Destino: ${branch}. Nenhum cadastro real será realizado.</div><button class="primary" data-action="simulate">Simular confirmação</button>`)};if(page==='sms'){$('#message').oninput=()=>{$('#message-preview').textContent=$('#message').value||'Sua mensagem aparecerá aqui.';$('#chars').textContent=`${$('#message').value.length} / 160`};$('#sms-form').onsubmit=e=>{e.preventDefault();modal('<h2>Revisar mensagem</h2><p>Mensagem preparada em ambiente de demonstração.</p><div class="preview-message" id="review-message"></div><p class="sub">Nenhum SMS será enviado.</p>');$('#review-message').textContent=$('#message').value}}if(page==='config')$('#reduce').onchange=e=>document.body.classList.toggle('reduce',e.target.checked)}
document.addEventListener('click',e=>{const el=e.target.closest('[data-go],[data-detail],[data-action]');if(!el)return;if(el.dataset.go){location.hash=el.dataset.go;return}if(el.dataset.detail){const r=rows.find(r=>r.serial===el.dataset.detail);modal(`<span class="tag">REGISTRO FICTÍCIO</span><h1>${r.serial}</h1><p class="sub">Detalhes do equipamento</p><div class="setting"><span>Modelo</span><b>${r.model}</b></div><div class="setting"><span>Filial</span><b>${r.branch}</b></div><div class="setting"><span>Operadora</span><b>${r.operator}</b></div><div class="setting"><span>Status</span>${badge(r.status)}</div>`);return}const action=el.dataset.action;if(action==='report')modal('<h2>Exportar relatório</h2><p>Escolha o formato para visualizar a interação.</p><div class="actions"><button data-action="export">Documento PDF</button><button data-action="export">Planilha XLSX</button></div><p class="sub">Exportação demonstrativa: nenhum arquivo operacional será gerado.</p>');if(action==='new')modal('<h2>Novo equipamento</h2><p class="sub">Exemplo da apresentação do formulário existente.</p><form id="new-form"><div class="fields"><label>Identificação<input required placeholder="DEMO-0100"></label><label>Modelo<select><option>RS300</option><option>ST310</option></select></label></div><button class="primary">Simular cadastro</button></form>');if(action==='new')$('#new-form').onsubmit=e=>{e.preventDefault();$('#dialog').close();toast('Simulação concluída. Nenhum dado real foi salvo.')};if(action==='config')modal('<h2>Configuração da integração</h2><div class="callout">Campos de conexão permanecerão nas configurações atuais. Nenhuma credencial é exposta nesta prévia.</div>');if(action==='simulate'||action==='export'){$('#dialog').close();toast('Interação demonstrativa concluída. Nenhum dado real foi alterado.')}});
$('#close').onclick=()=>$('#dialog').close();$('#exit').onclick=()=>modal('<h2>Sair da prévia?</h2><p>Você pode fechar esta aba. O Grupo RS Central não será encerrado.</p>');$('#branch').onchange=e=>{branch=e.target.value;render();toast(`Prévia da filial ${branch}`)};window.addEventListener('hashchange',render);render();
